import 'package:flutter/material.dart';

class PatientsDeleteDetails extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('حذف بيانات المرضى'),
      ),
      body: const Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            // Add delete form components here
          ],
        ),
      ),
    );
  }
}
